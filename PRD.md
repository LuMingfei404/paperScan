# PaperSnap（论文快闪）App 需求说明书

> 自用版 v1.0 ｜ 2026-08-22

## 1. 文档说明

| 项目 | 内容 |
| --- | --- |
| 产品名 | PaperSnap（论文快闪） |
| 平台 | Android（Kotlin + Jetpack Compose） |
| 使用者 | 仅本人（单用户，无账号体系） |
| 文档用途 | 直接指导开发的需求说明书：页面原型、数据结构、接口、交互、验收标准 |
| 配套物料 | 抓取/总结脚本、定时任务、Android 工程、网页交互原型（prototype/） |

## 2. 产品概述

### 2.1 一句话定位

一个每天早上自动更新、用一句话讲清"论文解决了什么问题、能做什么应用"的学术速览 App，让用户 10 分钟刷完当日最新论文。

### 2.2 核心场景

- 场景 1（晨间速览）：早上打开 App，上下滑动刷当天新论文，3 秒理解一篇。
- 场景 2（灵感沉淀）：看到感兴趣的论文右滑收藏，周末翻收藏夹找可做的应用方向。
- 场景 3（深度追踪）：对某篇感兴趣，点"查看原文"跳 arXiv 精读。
- 场景 4（深入讨论）：对某篇论文真正感兴趣时，点进详情页，与 AI 对话，搞清楚方法细节、可行性，以及还能衍伸出什么应用。

### 2.3 非目标（自用场景明确不做）

- 不做账号/登录/多设备云同步
- 不做社区、评论、点赞
- 不做个性化推荐算法（初期只做领域筛选）
- 不做论文全文解析、笔记批注（V1 不做，保留扩展）
- 不做 iOS 版本

### 2.4 成功标准

- 每天定时生成 ≥20 篇精选论文，每篇都有一句话总结（像摘要一样自然通顺）
- 打开 App 即可看到最新日期内容，上下滑动流畅
- 10 分钟内可完成当天全部浏览
- 断网时仍可浏览已缓存内容

## 3. 系统架构

### 3.1 总体架构

```
┌───────────────┐  每日定时   ┌──────────────┐
│ GitHub Actions│ ─────────▶ │  Python 脚本  │
└───────────────┘            └──────────────┘
                                     │ 抓取 arXiv API
                                     ▼
                              ┌──────────────┐
                              │ arXiv 元数据   │
                              └──────────────┘
                                     │ 送入 LLM
                                     ▼
                              ┌──────────────┐
                              │ LLM 提炼 JSON │
                              └──────────────┘
                                     │ 生成 papers JSON
                                     ▼
                              ┌──────────────┐
                              │ GitHub Pages /│
                              │ 静态文件托管   │
                              └──────────────┘
                                     ▲ HTTP GET
                              ┌──────────────┐
│ Android App  │
│ (Room 本地缓存)│
                              └──────────────┘
```

### 3.2 推荐方案（自用零成本）：GitHub Actions + 静态 JSON

- Python 脚本每天北京时间 08:00 触发
- 流程：抓 arXiv → LLM 总结 → 生成 `papers/2026-08-22.json` 与 `latest.json`
- 提交到仓库，通过 GitHub Pages / raw 链接供 App 拉取
- 优点：无服务器费用；内容管线无需服务器数据库（手机端本地用 Room）；手机端不持有任何 API Key
- 约束：GitHub Actions 免费额度足够（每天一次、几分钟）
- 已交付实现：`backend/papersnap.py`（arXiv 抓取 + LLM 一句话总结）、`.github/workflows/daily-papers.yml`，本地运行产出 `data/papers/` JSON（含 latest.json）
- App 已支持：动态日期列表（取自数据）、设置页配置数据源 URL、内置最新真实数据

### 3.3 备选方案：轻量 FastAPI（如不想用 GitHub）

- 本地/低配云服务器跑定时任务 + FastAPI 接口 `GET /api/v1/papers?date=`
- 适用于后续想加多用户、个性化或 AI 追问的情况

### 3.4 关键架构决策

| 决策点 | 选择 | 原因 |
| --- | --- | --- |
| LLM 调用位置 | 后端脚本 | 手机端保存 API Key 不安全 |
| 手机端职责 | 拉 JSON、渲染、本地收藏 | 简单、稳定 |
| 数据存储 | 服务端：静态 JSON（MVP）；手机端：Room（SQLite） | 服务端是批量生成+只读分发，静态文件足够；手机端收藏/对话/缓存是结构化数据，Room 更可靠（见 3.6） |
| AI 讨论调用 | 推荐 LLM 代理（见 3.5），MVP 可接受直连 | 避免 Key 进 APK；直连仅限自用 |

### 3.5 AI 讨论的接入方案（详情页聊天）

聊天需要实时调用 LLM，无法靠每天一次的静态 JSON 实现。自用场景三种方案：

| 方案 | 做法 | 优点 | 缺点 |
| --- | --- | --- | --- |
| A. Serverless 代理（推荐） | Cloudflare Workers / Vercel 函数包装 LLM API，App 只存代理地址 | Key 不进 App；免费额度够自用；无需常开电脑 | 需要一次配置 |
| B. 本地 FastAPI 代理 | 电脑上跑一个转发接口 | 零额外成本、完全可控 | 电脑关机时不可用 |
| C. App 直连 LLM API | App 内配置 Key 直接调用 | 实现最快 | Key 在 APK 内；仅自用不发布时可接受 |

推荐路径：先用 C 快速跑通聊天体验、验证价值，再迁到 A。

### 3.6 数据库决策（从业务出发）

数据库不是"能用就用"，也不是"刻意不用"，而是按数据特征决定：

- **手机端：用 Room（SQLite）**。收藏、隐藏、对话历史、离线缓存本质是结构化数据，需要索引与搜索（收藏夹关键词搜索）、事务一致性、版本迁移；后续加笔记、标签、按应用方向归类、阅读统计都顺。Room 是 Android 标准方案，开发成本与 DataStore 相当。
- **服务端：MVP 阶段不用数据库**。内容管线是"每天批量生成一次、App 只读分发"，静态 JSON 是最适合的形态。引入服务端数据库（如 PostgreSQL）的触发条件：多设备同步用户数据、内容治理（换模型后批量重总结/质量回填）、多用户、实时个性化。升级时保持接口不变，App 无感。

结论：MVP = 手机端 Room + 服务端静态 JSON；数据库按需引入，不设限制。

## 4. 内容策略

### 4.1 数据源

- 主数据源：arXiv API（按分类、按提交日期增量抓取）
- 暂不接入其他源（PubMed/Crossref 留作 V2）

### 4.2 领域与数量

- 默认订阅（可在脚本配置与 App 设置中调整）：cs.AI、cs.CV、cs.CL、cs.LG、cs.RO、cs.SE
- 每日每领域按提交时间取最新 N 篇，总量控制在 30~50 篇/天
- 去重规则：同一论文出现在多个分类只保留一次；arxiv_id 唯一

### 4.3 过滤规则（V1 简单规则）

- 排除已入库论文（按 arxiv_id 去重）
- 可选关键词黑名单（如纯理论、与兴趣无关的分类）
- 数量上限优先保证"当天最新"，超过上限截断

### 4.4 成本预算

- 模型：DeepSeek-V3 或 GPT-4o-mini 级别
- 每篇约 1~2k token（输入 Title+Abstract，输出两句话）
- 每天 50 篇约 100k token 以内，成本远低于 1 元/天
- 脚本设置硬性上限（每天最多 100 篇、单篇输出 200 token），防止失控

## 5. 功能需求

### 5.1 功能清单

| 编号 | 模块 | 说明 | 优先级 |
| --- | --- | --- | --- |
| F1 | 数据抓取与生成 | 定时抓 arXiv，生成当日 JSON | P0 |
| F2 | AI 提炼 | LLM 生成一句话 summary，JSON 校验 | P0 |
| F3 | 每日卡片流 | 上下滑动浏览当日论文 | P0 |
| F4 | 查看原文 | WebView 打开 arXiv 页面/PDF | P0 |
| F5 | 本地收藏 | Room 存储收藏，重启不丢 | P0 |
| F6 | 离线缓存 | Room 缓存最近 30 天数据，断网可读 | P0 |
| F7 | 领域筛选 | 按分类过滤已加载内容 | P1 |
| F8 | 左滑隐藏 | 标记不感兴趣，当日内不再显示 | P1 |
| F9 | 深色模式 | 跟随系统，两套配色 | P1 |
| F10 | 收藏夹搜索 | 按标题/一句话总结关键词搜索 | P2 |
| F11 | 分享海报 | 生成卡片长图存相册/分享 | P2 |
| F12 | 论文详情页 | 展示论文完整摘要、作者、元数据，入口进入 AI 讨论 | P0 |
| F13 | AI 讨论（详情页内对话） | 围绕论文与 AI 对话，支持建议问题与流式回复，历史 Room 保存 | P0 |

### 5.2 详细功能说明

**F1 数据抓取与生成**

- 用 arxiv 库按分类查询最近 1 天新增，字段：arxiv_id、标题、摘要、作者、分类、发表日期、PDF 链接
- 生成 JSON 前做完整性校验，缺字段的条目丢弃并记录日志

**F2 AI 提炼**

- 输入：标题 + 摘要（截断至 1500 token）
- 输出：`{"summary": "..."}`，一句话自然概括（不超过 80 字，涵盖解决的问题与可能应用，像摘要一样通顺）
- JSON 解析失败重试 2 次；仍失败则该篇降级：summary 取原文摘要首句，标记 quality=low

**F3 每日卡片流**

- 首页默认加载 latest.json；顶部日期可切换历史日期（仅限本地有缓存的日期）
- 下拉刷新重新拉取；滑到当天末尾提示"今天看完了"

**F4 查看原文**

- 卡片按钮唤起 WebView 打开 arXiv abs 页面；提供"在浏览器打开"

**F5 本地收藏**

- 支持收藏、取消收藏；收藏状态图标即时反馈
- 收藏列表存入 Room（papers 表 is_bookmarked 字段），不依赖服务端

**F6 离线缓存**

- 网络请求成功即写入 Room 缓存；按日期维度存储；设置页可手动清理缓存

**F7 领域筛选**

- 筛选页勾选分类，保存后首页仅显示所选分类（本地过滤，不影响已下载数据）

**F8 左滑隐藏**

- 隐藏列表写入本地，同一天不再出现；不上报服务端（自用）

**F9 深色模式**

- 跟随系统；深浅两套色板，见 8.1

**F10 收藏夹搜索**

- 对收藏的标题与一句话总结做子串匹配

**F11 分享海报**

- 将论文卡片渲染为长图（Compose Canvas），保存相册或调起系统分享

**F12 论文详情页**

- 从首页卡片点击进入；展示完整摘要、作者、分类、发表日期、PDF 链接、一句话总结
- 顶部返回；提供"复制总结"、"查看 PDF"快捷操作
- 页面下半部分为 AI 讨论区（见 F13）

**F13 AI 讨论（详情页内对话）**

- 围绕当前论文与 AI 对话：系统自动携带该论文的标题与摘要作为上下文
- 提供建议问题快捷入口（核心创新点 / 与之前方法对比 / 能做什么应用 / 实现难度）
- 回复流式显示（打字机效果）；支持中断生成、失败重发
- 每篇论文的对话历史存入 Room（chat_messages 表），再次进入可继续
- 上下文窗口上限：单篇对话最多保留最近 20 轮，超出丢弃最早消息
- 接入方式按 3.5 三选一；直连方案下在设置页配置 API Key 与模型名

## 6. 数据结构与接口

### 6.1 服务端 JSON（papers/YYYY-MM-DD.json）

```json
{
  "date": "2026-08-22",
  "generated_at": "2026-08-22T08:00:00+08:00",
  "schema_version": 1,
  "total": 2,
  "papers": [
    {
      "arxiv_id": "2408.12345v1",
      "title": "Efficient On-Device Diffusion Model for Video Super-resolution",
      "title_zh": "手机端实时视频超分的轻量扩散模型",
      "categories": ["cs.CV", "cs.AI"],
      "authors": ["Alice Zhang"],
      "published": "2026-08-21T17:00:00Z",
      "pdf_url": "https://arxiv.org/pdf/2408.12345v1",
      "abstract": "We propose ...",
      "summary": "提出一种轻量扩散架构，让手机端也能实时完成 720p 视频超分与风格化，可用来开发视频增强相机或直播滤镜。"
    }
  ]
}
```

字段说明：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| date | string | 是 | 论文所属日期（北京时间） |
| generated_at | string | 是 | 生成时间（ISO 8601） |
| schema_version | int | 是 | 数据结构版本，当前为 1 |
| total | int | 是 | 论文数量 |
| papers[].arxiv_id | string | 是 | 唯一标识（如 2408.12345v1） |
| papers[].title | string | 是 | 原文标题 |
| papers[].title_zh | string | 否 | 机器翻译标题，可为空 |
| papers[].categories | string[] | 是 | 分类列表 |
| papers[].authors | string[] | 否 | 作者列表 |
| papers[].published | string | 是 | 发表日期（ISO 8601） |
| papers[].pdf_url | string | 是 | PDF/详情页链接 |
| papers[].abstract | string | 是 | 摘要原文（详情展开用） |
| papers[].summary | string | 是 | 一句话总结（≤80 字，自然通顺，涵盖解决的问题与应用） |

### 6.2 latest.json

同结构，指向最近一期；App 启动时请求它。

### 6.3 接口定义

| 接口 | 说明 |
| --- | --- |
| GET /papers/latest.json | 最新一期数据 |
| GET /papers/{date}.json | 指定日期数据（历史缓存场景） |
| POST /chat（方案 A/B 时） | 携带论文 arxiv_id 与问题，代理转发 LLM 并返回流式回复 |

自用场景为公开静态 JSON，无鉴权；/chat 仅在启用代理方案时存在（方案 C 直连则 App 直接调 LLM）。

### 6.4 手机端本地存储方案（Room）

papers 表：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| arxiv_id | TEXT PK | 论文唯一 ID |
| date | TEXT | 所属日期 |
| title / title_zh | TEXT | 标题 |
| categories | TEXT | 分类 JSON 数组字符串 |
| authors | TEXT | 作者 JSON 数组字符串 |
| published | TEXT | 发表日期 |
| pdf_url | TEXT | 原文链接 |
| abstract | TEXT | 摘要 |
| summary | TEXT | 一句话总结 |
| is_bookmarked | INTEGER | 0/1 是否收藏 |
| is_hidden | INTEGER | 0/1 是否隐藏 |
| fetched_at | INTEGER | 缓存时间戳 |

settings 表：key/value（selected_categories、dark_mode、cache_days）。

chat_messages 表：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER PK | 自增主键 |
| arxiv_id | TEXT | 关联论文 |
| role | TEXT | user / assistant |
| content | TEXT | 消息内容 |
| created_at | INTEGER | 时间戳 |

为什么手机端用 Room：收藏搜索、对话历史、离线缓存都需要索引、事务与版本迁移；后续加字段（标签、笔记、阅读状态）只需一次数据库迁移。为什么服务端 MVP 不用数据库：见 3.6。

## 7. AI 提炼流水线

### 7.1 Prompt 模板

```
你是一名资深技术产品经理。根据下面论文的标题和摘要，用最通俗的中文写一句话总结（不超过80字）：
要求：总结要像摘要一样自然通顺，一句话同时点明论文解决了什么问题、能用来开发什么应用；禁止分条列举、禁止学术套话。
要求：严格输出 JSON：{"summary":"..."}
标题：{title}
摘要：{abstract}
```

### 7.2 质量与容错

- 强制 JSON 解析；失败重试 2 次（更换采样参数）
- 每篇独立调用，单篇失败不影响其他篇
- 输出为空/超长的篇目标记 quality=low，App 端降级展示
- 每日汇总日志：成功/失败/降级数量，便于自查

## 8. 页面原型

### 8.1 全局设计规范

> 已交付可运行网页交互原型：`prototype/`（`node server.mjs` 启动，浏览器打开 http://127.0.0.1:8360）。原型即为本节与 8.2~8.9 所描述界面的实际交互实现，后续 Android 端按此还原。原型用 localStorage 模拟真机本地持久化，对应 Android 端的 Room 收藏/隐藏/对话历史。

- 主色：Teal #0D9488，强调色 #14B8A6
- 深色模式：背景 #0F172A、卡片 #1E293B；浅色模式：背景 #F8FAFC、卡片 #FFFFFF
- 标题 20sp 粗体、正文 15sp、标签 12sp；卡片圆角 20dp、间距 12dp
- 底部导航 3 项：首页 / 收藏 / 设置
- 中文默认系统字体，英文数字用默认字体

### 8.2 首页（每日速递流）

```
┌────────────────────────────────┐
│ PaperSnap    [2026-08-22 ▾]   │ ← 顶部栏：logo、日期(可点)、
│              [⏳] [⚙]        │    刷新、设置入口
├────────────────────────────────┤
│   ┌────────────────────────┐  │
│   │ [cs.CV] [cs.AI] · 3h   │  │ ← 标签：分类、发布时间
│   │ arXiv:2408.12345       │  │
│   │                        │  │
│   │ 手机端实时视频超分      │  │ ← 标题（中文，可看英文）
│   │ 的轻量扩散模型          │  │
│   │                        │  │
│   │ 📄 一句话总结           │  │
│   │ 提出轻量扩散架构，让手  │  │
│   │ 机端实时完成视频超分与  │  │
│   │ 风格化，可做增强相机…   │  │
│   │                        │  │
│   │ [查看原文 ↗]   [📤]    │  │ ← 查看原文、分享
│   └────────────────────────┘  │
│   ┌────────────────────────┐  │
│   │  (下一篇卡片预览)        │  │ ← 上滑进入下一篇
│   └────────────────────────┘  │
│                                │
│ [🏠 首页]  [⭐ 收藏]  [⚙️]   │ ← 底部导航
└────────────────────────────────┘
```

手势：上滑/下滑翻篇（Pager 吸附）；右滑收藏；左滑隐藏；长按弹浮层；点击卡片进入论文详情页。

### 8.3 领域筛选页

```
┌────────────────────────────────┐
│ ← 返回        关注领域       ✓ │
├────────────────────────────────┤
│ 勾选你关注的领域（本地保存）     │
│                                │
│ [✓ Machine Learning]  [ CV ]   │
│ [✓ NLP]            [Robotics]  │
│ [Graphics]          [ HCI ]    │
│ [Data Mining]      [ SE ]      │
│ [✓ AI for Science] [Quantum]   │
│                                │
│ ┌────────────────────────────┐ │
│ │          确认选择            │ │
│ └────────────────────────────┘ │
└────────────────────────────────┘
```

说明：全不选 = 全部显示；选中状态即时反馈。

### 8.4 收藏夹

```
┌────────────────────────────────┐
│ 我的灵感库          [🔍] [📤] │
├────────────────────────────────┤
│ 卡片1: 轻量ViT在智能眼镜        │
│   📄 把ViT压缩到眼镜可常开运行…│
│ 卡片2: 生成式3D快速重建         │
│   📄 单图0.5秒生成3D资产…       │
│ 卡片3: IoT联邦学习优化          │
│   📄 通信裁剪让IoT也能联邦学习…│
│ （点卡片进入详情/取消收藏）      │
└────────────────────────────────┘
```

### 8.5 设置页

```
┌────────────────────────────────┐
│ 设置                            │
├────────────────────────────────┤
│ 深色模式         [跟随系统 ▾]   │
│ 数据源地址       (显示当前)      │
│ 缓存清理         [清理缓存]      │
│ 当前版本         v1.0.0         │
│ 关于/免责声明                    │
└────────────────────────────────┘
```

### 8.6 原文 WebView

- 顶部标题栏显示 arXiv 页面标题；底部工具条："在浏览器打开"、"返回"
- 加载失败显示重试；支持点击 PDF 链接

### 8.7 长按浮层

```
┌────────────────────────────────┐
│ 复制一句话总结                  │
│ 复制标题                       │
│ 在浏览器打开                   │
│ 取消                           │
└────────────────────────────────┘
```

### 8.8 状态页

- 加载中：骨架卡片（灰色渐变占位）
- 空数据："今天还没有新论文 / 数据源未生成，请稍后再试 [刷新]"
- 错误但本地有缓存："网络不可用，展示的是缓存数据（日期）[重试]"
- 错误且无缓存："请联网加载一次后再离线使用"

### 8.9 论文详情页 + AI 讨论

```
┌────────────────────────────────┐
│ ← 返回        论文详情      [📤]│
├────────────────────────────────┤
│ [cs.CV] [cs.AI] arXiv:2408... │
│ 手机端实时视频超分的轻量扩散模型 │
│ 作者: Alice Zhang 等 · 08-21  │
│                                │
│ 📄 一句话总结                  │
│ 提出轻量扩散架构，让手机端实  │
│ 时完成视频超分与风格化，可做  │
│ 视频增强相机或直播滤镜…        │
│                                │
│ 摘要（完整，可展开/收起）        │
│ We propose ...                  │
│                                │
│ [查看 PDF] [复制总结]          │
│ ┌────────────────────────────┐ │
│ │ 💬 AI 讨论                 │ │
│ │ 建议问题：                  │ │
│ │ [核心创新点是什么？]        │ │
│ │ [和之前方法比强在哪？]      │ │
│ │ [我能用它做什么应用？]      │ │
│ │                            │ │
│ │ 你：适合做实时滤镜吗？      │ │
│ │ AI：可以，论文在端侧跑通了  │ │
│ │   视频级推理，滤镜只需加    │ │
│ │   风格化分支...             │ │
│ │                            │ │
│ │ [输入框................➤] │ │
│ └────────────────────────────┘ │
└────────────────────────────────┘
```

说明：上半部分为论文信息（可滚动），下半部分为固定聊天区；点建议问题自动发送；回复流式显示；同一论文再次进入时恢复历史对话。

## 9. 交互规范

### 9.1 手势表

| 手势 | 动作 | 反馈 |
| --- | --- | --- |
| 上滑 | 下一篇 | 卡片跟随位移 + 弹性吸附 |
| 下滑 | 上一篇 | 同上 |
| 右滑 | 收藏 | 卡片右移，爱心填充 + 震动 |
| 左滑 | 隐藏 | 卡片左移淡出 + 提示"已隐藏" |
| 长按 | 浮层菜单 | 底部弹出 |
| 点击日期 | 历史日期选择器 | 仅显示有本地缓存的日期 |
| 点击卡片 | 进入论文详情页 | 页面转场（从右滑入） |

### 9.2 动画

- 翻篇 250ms 曲线缓动；收藏/隐藏 200ms
- 收藏/隐藏操作带 Haptics 震动反馈

### 9.3 状态机

- Feed 数据状态：加载中 → 成功 / 空 / 错误；切换日期、下拉刷新复用同一状态机
- 收藏状态：未收藏 ↔ 已收藏（点击或右滑均可切换）

## 10. 非功能需求

- 性能：卡片滚动 60fps；冷启动 < 2s；JSON 解析在后台线程
- 网络：蜂窝/Wi-Fi 均可；请求失败自动回退缓存
- 离线：最近 30 天数据本地可读
- 聊天：流式返回；单次请求超时 30s；失败可一键重发
- 电量：无后台服务，仅前台拉取
- 安全：App 内不保存任何 LLM API Key
- 合规：遵守 arXiv API 使用规范（限速 ≤3-4 请求/秒）；数据仅个人使用

## 11. 里程碑与验收

| 里程碑 | 内容 | 验收标准 |
| --- | --- | --- |
| M1 内容管线（1-2 天） | 抓取 + LLM 脚本 | 一次运行产出 ≥20 篇含一句话 summary 的 JSON，人工抽查 5 篇质量合格 |
| M2 自动化（1-2 天） | GitHub Actions 定时任务 | 次日 8 点后 latest.json 自动更新 |
| M3 Android 主流程（4-6 天） | 卡片流 + 详情页 + AI 讨论 + 网络 + Room 缓存 | 满足 2.4 成功标准中的刷流与缓存，可进详情页与 AI 对话 |
| M4 体验完善（2-3 天） | 收藏/筛选/深色/分享 | 全部 P1 功能通过手测 |

MVP 最终验收清单：

1. 定时任务当天自动生成数据
2. App 打开显示最新日期，上下滑动流畅
3. 收藏/取消收藏后重启保留
4. 断网可看已缓存内容
5. 查看原文可打开 arXiv
6. 领域筛选生效
7. 点击卡片进入详情页，可与 AI 围绕论文对话且历史不丢

## 12. 待确认决策（开发前需定）

1. 每日篇数与领域清单（默认 30~50 篇、6 个领域）
2. 中文标题是否翻译（title_zh 留空则不翻译）
3. 分享海报是否需要（P2，可后置）
4. AI 讨论接入方案：先直连验证体验，再迁 Serverless 代理（见 3.5）
5. 一句话总结的字数与风格（默认 ≤80 字、摘要式通顺句，涵盖问题与应用）
